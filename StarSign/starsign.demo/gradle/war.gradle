/*
* war gradle
*
*/
apply plugin: 'war'

// Application Version
war {
	baseName = 'starsign-engine'
	version = '1.0'
	
	classpath {
		
	}
}

// Exclude directory(WEB-INF) from resource in Gradle
sourceSets {
	main {
		resources {
			exclude '**/WEB-INF/*'
			exclude 'WEB-INF'
		}
	}	
}